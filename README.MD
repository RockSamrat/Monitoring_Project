# Flask + Prometheus + Grafana Monitoring Setup

A simple example demonstrating how to monitor a Flask application using Prometheus and Grafana with Docker Compose.

## Commands
```bash
docker-compose up --build

